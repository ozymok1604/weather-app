const initialState = {
  isLoading: false,
};

const wheatherReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default wheatherReducer;
